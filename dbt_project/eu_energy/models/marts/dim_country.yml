version: 2

models:
  - name: dim_country
    description: "Country dimension from long fact."
    columns:
      - name: country_key
        tests: [not_null, unique]
      - name: country
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: ["nl", "de"] # adjust later if you add more
